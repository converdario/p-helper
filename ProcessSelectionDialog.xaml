<Window x:Class="PHelper.ProcessSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PHelper"
        Title="Select Process" Height="350" Width="450" WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize">
    
    <Window.Resources>
        <local:IconConverter x:Key="IconConverter"/>
    </Window.Resources>

    <Border Background="{StaticResource BgDark}" CornerRadius="8" BorderBrush="{StaticResource BorderLight}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Barra del Titolo -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseDown">
                <TextBlock Text="Select Process" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="13" FontWeight="SemiBold"/>
            </Grid>

            <!-- Contenuto Centrale con Checkbox -->
            <ListBox Name="ProcessListBox" Grid.Row="1" Margin="15,5,15,10" 
                     Background="{StaticResource BgPanel}" 
                     BorderBrush="{StaticResource BorderLight}" 
                     BorderThickness="1" 
                     Foreground="{StaticResource TextWhite}" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="4,6">
                            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0" />
                            <Image Source="{Binding FullPath, Converter={StaticResource IconConverter}}" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center" />
                            <TextBlock VerticalAlignment="Center">
                                <Run Text="{Binding ProcessName}" />
                                <Run Text=" - " Foreground="#888888"/>
                                <Run Text="{Binding WindowTitle}" Foreground="#AAAAAA"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <!-- Controlli in Basso -->
            <Grid Grid.Row="2" Margin="15,0,15,15">
                <Button Content="Browse..." Width="90" Height="32" HorizontalAlignment="Left" Click="BrowseButton_Click" />
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="OK" Width="80" Height="32" Margin="0,0,10,0" Click="Ok_Click" />
                    <Button Content="Cancel" Width="80" Height="32" Click="Cancel_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>